public int bsearch(int[] array, int X) {  //Функция принимает 2 параметра, отсортированный массив и целое число
        if (array.length != 0) {          //Проверяем на заполненность нашего массива, если он пустой, то озвращаем -1, т.к. такого индекса нет
            for (int i = array.length - 1; i >= 0; i--) {   //Вызываем цикл, в котором инициализируем переменную i значением последнего индекса и запускаем цикл, до последнего элемента в массиве
                if (array[i] < X) { break;  //Т.к. массив отсортирован по убыванию, начинаем сравнивать элементыс конца и как только находим - возвращаем индекс
                 return i;} // и так же стопаем цикл, но хватило бы и одного ретурна, т.к. функция сама бы прекратила работу
            }
            return -1;  //Если такого элемента не нашло, функция снова отправляет -1
        } else {
            return -1;
        }
}
